---
import BaseLayout from '../../layouts/BaseLayout.astro';
import AdSense from '../../components/AdSense.astro';
import { getCollection } from 'astro:content';
import { getDynamicCategories } from '../../utils/categories';
import { getCategorySlug } from '../../utils/slug';
import siteConfig from '../../config/site';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const { allCategories, totalPosts } = await getDynamicCategories(100);

const allPosts = await getCollection('blog', ({ data }) => {
  return import.meta.env.PROD ? !data.draft : true;
});

// Extract recent 3 posts per category
const categoriesWithRecentPosts = allCategories.map(cat => {
  const posts = allPosts
    .filter(p => getCategorySlug(p.data.category) === cat.slug)
    .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
    .slice(0, 3);
  return {
    ...cat,
    recentPosts: posts,
  };
});
---

<BaseLayout
  title="All Categories & Tiers | K-Pop Hangul"
  description="Explore all Korean language learning categories, proficiency tiers, and music genres on K-Pop Hangul."
>
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <!-- Header -->
    <div class="text-center max-w-2xl mx-auto mb-12">
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-indigo-50 text-indigo-700 text-xs font-semibold mb-3 border border-indigo-100">
        <span>📑 Category Directory</span>
      </div>
      <h1 class="text-3xl sm:text-4xl font-extrabold text-slate-900 tracking-tight">
        Learning Categories & Tiers
      </h1>
      <p class="text-slate-600 text-sm sm:text-base mt-2">
        Browse {allCategories.length} categories across {totalPosts} Korean song lessons from Melon and Spotify.
      </p>
    </div>

    <AdSense slotId="1234567890" type="top-banner" />

    <!-- Categories Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 my-10">
      {categoriesWithRecentPosts.map((cat, idx) => (
        <div class="bg-white rounded-2xl border border-slate-200/80 p-6 shadow-xs hover:shadow-md hover:border-indigo-300 transition-all flex flex-col justify-between">
          <div>
            <div class="flex items-center justify-between gap-3 mb-4">
              <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-xl bg-indigo-50 text-indigo-700 text-xs font-bold border border-indigo-100">
                #{idx + 1}
              </span>
              <span class="text-xs font-semibold text-slate-500 bg-slate-100 px-2.5 py-0.5 rounded-full">
                {cat.count} {cat.count === 1 ? 'song' : 'songs'}
              </span>
            </div>

            <h2 class="text-xl font-bold text-slate-900 mb-2">
              <a href={cat.href} class="hover:text-indigo-600 transition-colors">
                {cat.name}
              </a>
            </h2>

            <!-- Recent 3 Articles in Category -->
            <div class="mt-4 pt-4 border-t border-slate-100 space-y-2.5">
              <div class="text-[11px] font-semibold text-slate-400 uppercase tracking-wider">Recent Lessons</div>
              {cat.recentPosts.map(post => (
                <a
                  href={`${base}/blog/${post.slug}/`}
                  class="block text-xs text-slate-600 hover:text-indigo-600 transition-colors line-clamp-1 group"
                >
                  <span class="text-slate-300 group-hover:text-indigo-400 mr-1.5">•</span>
                  {post.data.title}
                </a>
              ))}
            </div>
          </div>

          <div class="mt-6 pt-4 border-t border-slate-100">
            <a
              href={cat.href}
              class="inline-flex items-center gap-1 text-xs font-semibold text-indigo-600 hover:text-indigo-700 hover:gap-1.5 transition-all"
            >
              <span>Explore all in {cat.name}</span>
              <span>&rarr;</span>
            </a>
          </div>
        </div>
      ))}
    </div>

    <div class="mt-12">
      <AdSense slotId="3456789012" type="multiplex" />
    </div>
  </div>
</BaseLayout>

